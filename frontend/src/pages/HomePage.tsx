import React from "react"
import NavBar from "./NavBar"

const HomePage = () => {
    return (
        <main>
            <NavBar/>
            <h1 style={{textAlign: "center", paddingTop: "2rem"}}>Home Page</h1>
        </main>
    )
}

export default HomePage
